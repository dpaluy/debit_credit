---
en:
  activerecord:
    errors:
      models:
        debit_credit/entry:
          unbalanced: item amounts must balance to 0
          already_inversed: parent transaction already inversed
        debit_credit/account:
          overdraft: overdraft is not allowed

